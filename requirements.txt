# Install dependencies from pyproject.toml
# Usage: pip install -e .
#
# For development dependencies: pip install -e ".[dev]"
# For ML dependencies: pip install -e ".[ml]"
# For all dependencies: pip install -e ".[dev,ml]"

-e .
